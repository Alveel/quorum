# Stub values for helm lint: OpenShift Route + openshift-oauth-proxy (default scenario)
ingress:
  type: route
  host: quorum.apps.cluster.example.com

proxy:
  provider: openshift
  tls:
    secretName: ""
  cookieSecret:
    secretName: quorum-oauth-cookie
    secretKey: cookie-secret

admin:
  groups: [quorum-admins]

database:
  secretName: quorum-db-url
  secretKey: DATABASE_URL
  tlsCA:
    enabled: false
    secretName: ""
