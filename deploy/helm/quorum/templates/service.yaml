apiVersion: v1
kind: Service
metadata:
  name: {{ include "quorum.fullname" . }}
  labels:
    {{- include "quorum.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "quorum.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: proxy
